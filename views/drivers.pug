extends layout
block content
    h2 Drivers
    p There are #{passengers.length} drivers waiting.

    ol
        each driver in drivers
            li
                a(href=`/drivers/${driver.id}`)= driver.name

